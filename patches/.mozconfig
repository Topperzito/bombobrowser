# Build config for BomboBrowser
. $topsrcdir/browser/config/mozconfig

# Custom branding
ac_add_options --with-branding=browser/branding/bombo

# Release mode, no debug/tests
ac_add_options --enable-release
ac_add_options --disable-debug
ac_add_options --disable-tests

# Disable crash reporter and updater
ac_add_options --disable-crashreporter
ac_add_options --disable-updater

# Telemetry off
MOZ_TELEMETRY_REPORTING=
MOZ_CRASHREPORTER=
MOZ_UPDATER=
MOZ_DATA_REPORTING=

# Optimizations
ac_add_options --enable-optimize="-O2"

# WASM sandboxed libraries need WASI headers (not available on CI)
ac_add_options --without-wasm-sandboxed-libraries

# Node.js is optional (uses system-installed if present)
ac_add_options --disable-nodejs

# Use system toolchain (don't try to install packages)
ac_add_options --disable-bootstrap